<!--
Template for mabl-app-context-crawl. Fill every {{placeholder}} from the browser crawl.
Delete the "Additional context" section if no --sources were provided.
Keep it scannable: bullets/tables over prose. This is agent instructions, not docs.
-->
# {{APP_NAME}} — Application context for mabl test creation

> Audience: mabl's test-creation agent. This describes the app **as seen in the browser** (client-side only). Use it to understand pages, personas, and user flows when authoring tests.
> Generated by the `mabl-app-context-crawl` skill — black-box crawl of `{{CRAWL_URL}}`. Personas crawled: {{PERSONAS_CRAWLED}}. Env mode: {{ENV_MODE}} (read-only / safe-env). Crawl depth: {{MAX_DEPTH}}.

## 1. Overview
- **What it is:** {{ONE_LINE_PURPOSE}}
- **Theme / domain:** {{THEME}}
- **Base URL (dev):** {{BASE_URL}}
- **Environments:** {{ENVIRONMENTS}}
- **Front-end stack:** {{STACK}}

## 2. Personas & credentials
| Persona | Login | Password | Lands on | Can access |
|---|---|---|---|---|
{{PERSONA_ROWS}}

## 3. Page / route map
| Route | Page | Purpose | Visible to |
|---|---|---|---|
{{ROUTE_ROWS}}

## 4. Core user flows
{{FLOWS}}
<!-- Each flow:
### {{flow name}}  (persona: {{persona}})
1. Step …
2. Step …
- **Asserts:** {{what a test should verify}}
-->

## 5. UI conventions & selector hints
{{SELECTOR_HINTS}}

## 6. Known quirks / things to avoid
{{QUIRKS}}

## 7. Coverage & gaps
<!-- Black-box blind spots. Be honest so the mabl agent knows what this doc does NOT cover. -->
- **Routes/areas not reached:** {{NOT_REACHED}}
- **Flows inferred but not executed:** {{INFERRED_FLOWS}}
- **Role/access notes:** {{ACCESS_NOTES}}
- **Possible runtime variants not exercised** (feature flags, A/B, time-dependent UI): {{RUNTIME_VARIANTS}}

## 8. Additional context
<!-- Only present when --sources was provided. Business rules the UI can't reveal. Cite each source. -->
{{ADDITIONAL_CONTEXT}}
